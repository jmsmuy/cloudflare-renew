name: OpenWrt Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      target:
        description: 'OpenWrt target architecture (e.g., x86_64, arm_cortex-a9, ramips/mk7621)'
        required: false
        default: 'x86_64'
      sdk_version:
        description: 'OpenWrt SDK version (e.g., 23.05.0, 22.03.5)'
        required: false
        default: '24.10.2'

permissions:
  contents: write

jobs:
  build-openwrt:
    name: Build OpenWrt Binaries
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [ramips]
        subtarget: [mt7621]
        sdk_version: [24.10.2]
        gcc_version: [13.3.0]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Build OpenWrt binaries
      run: |
        echo "🔨 Building for OpenWrt ${{ matrix.sdk_version }} - ${{ matrix.target }}"
        
        # Create build directory
        mkdir -p build/openwrt-${{ matrix.sdk_version }}-${{ matrix.target }}
        
        # Create Dockerfile for this specific target
        cat > Dockerfile.openwrt << 'EOF'
        FROM ubuntu:22.04
        
        # Install dependencies
        RUN apt-get update && apt-get install -y \
            build-essential \
            wget \
            tar \
            xz-utils \
            zstd \
            ca-certificates \
            && rm -rf /var/lib/apt/lists/*
        
        # Set working directory
        WORKDIR /build
        
        # Download and extract OpenWrt SDK
        RUN wget -O openwrt-sdk.tar.zst \
            "https://downloads.openwrt.org/releases/${{ matrix.sdk_version }}/targets/${{ matrix.target }}/${{ matrix.subtarget }}/openwrt-sdk-${{ matrix.sdk_version }}-${{ matrix.target }}-${{ matrix.subtarget }}_gcc-${{ matrix.gcc_version }}_musl.Linux-x86_64.tar.zst" \
            || echo "Failed to download SDK for ${{ matrix.target }}/{{ matrix.subtarget }} on ${{ matrix.sdk_version }}"
        
        # Extract SDK
        RUN tar -xf openwrt-sdk.tar.zst && \
            mv openwrt-sdk-* openwrt-sdk
        
        # Create package
        RUN mkdir -p openwrt-sdk/package
        RUN mkdir -p openwrt-sdk/package/cloudflare-renew
        RUN mkdir -p openwrt-sdk/package/cloudflare-renew/src
        RUN mkdir -p openwrt-sdk/package/cloudflare-renew/src/lib
        RUN mkdir -p openwrt-sdk/package/cloudflare-renew/src/tools
        RUN mkdir -p openwrt-sdk/package/cloudflare-renew/src/tests

        RUN cp cloudflare_renew.c Makefile openwrt-sdk/package/cloudflare-renew/src/
        RUN cp lib/json.c lib/json.h openwrt-sdk/package/cloudflare-renew/src/lib/
        RUN cp lib/cloudflare_utils.c lib/cloudflare_utils.h openwrt-sdk/package/cloudflare-renew/src/lib/
        RUN cp lib/socket_http.c lib/socket_http.h openwrt-sdk/package/cloudflare-renew/src/lib/
        RUN cp lib/publicip.c lib/publicip.h openwrt-sdk/package/cloudflare-renew/src/lib/
        RUN cp lib/getip.c lib/getip.h openwrt-sdk/package/cloudflare-renew/src/lib/
        RUN cp lib/setip.c lib/setip.h openwrt-sdk/package/cloudflare-renew/src/lib/
        RUN cp lib/http_utils.c lib/http_utils.h openwrt-sdk/package/cloudflare-renew/src/lib/
        RUN cp tools/getip.c openwrt-sdk/package/cloudflare-renew/src/tools/
        RUN cp tools/setip.c openwrt-sdk/package/cloudflare-renew/src/tools/
        RUN cp tools/publicip.c openwrt-sdk/package/cloudflare-renew/src/tools/
        
        RUN cp Makefile.openwrt openwrt-sdk/package/cloudflare-renew/Makefile
        
        RUN cd openwrt-sdk && make V=99
        
        # # Create tarball
        # RUN cd /build && tar -czf cloudflare-renew-openwrt-${{ matrix.sdk_version }}-${{ matrix.target }}.tar.gz package/
        
        # # Copy result to host
        # RUN cp /build/cloudflare-renew-openwrt-${{ matrix.sdk_version }}-${{ matrix.target }}.tar.gz /build/source/build/openwrt-${{ matrix.sdk_version }}-${{ matrix.target }}/
        # EOF
        
        # Build Docker image and run build
        # docker build -f Dockerfile.openwrt -t openwrt-builder-${{ matrix.target }}-${{ matrix.sdk_version }} .
        
        # Clean up Docker image
        # docker rmi openwrt-builder-${{ matrix.target }}-${{ matrix.sdk_version }}
        
        echo "✅ Build completed for ${{ matrix.target }}-${{ matrix.subtarget }} on ${{ matrix.sdk_version }}"
        
    - name: Upload OpenWrt binaries
      uses: actions/upload-artifact@v4
      with:
        name: openwrt-binaries-${{ matrix.sdk_version }}-${{ matrix.target }}
        path: build/openwrt-${{ matrix.sdk_version }}-${{ matrix.target }}/
        retention-days: 30
        
    - name: Create release assets
      if: github.ref == 'refs/heads/main'
      run: |
        echo "📦 Preparing OpenWrt release assets..."
        
        # Get version
        if [ -f VERSION ]; then
          VERSION="v$(cat VERSION)"
        else
          DATE=$(date +%Y.%m.%d)
          SHORT_SHA=$(git rev-parse --short HEAD)
          VERSION="v${DATE}-${SHORT_SHA}"
        fi
        
        # Check if release exists
        if gh release view "$VERSION" >/dev/null 2>&1; then
          echo "📤 Uploading OpenWrt binaries to existing release $VERSION"
          
          # Upload each OpenWrt binary package
          for pkg in build/openwrt-*/*.tar.gz; do
            if [ -f "$pkg" ]; then
              echo "📤 Uploading $pkg..."
              gh release upload "$VERSION" "$pkg" --clobber
            fi
          done
        else
          echo "⚠️  Release $VERSION does not exist yet. Run the release workflow first."
        fi
        
    - name: Build summary
      run: |
        echo "🏗️  OpenWrt Build Summary"
        echo "========================="
        echo "Target: ${{ matrix.target }}"
        echo "SDK Version: ${{ matrix.sdk_version }}"
        echo "Build Status: ✅ Success"
        echo ""
        echo "Built binaries:"
        ls -la build/openwrt-${{ matrix.sdk_version }}-${{ matrix.target }}/
        echo ""
        echo "Package:"
        ls -lh build/openwrt-${{ matrix.sdk_version }}-${{ matrix.target }}/*.tar.gz
